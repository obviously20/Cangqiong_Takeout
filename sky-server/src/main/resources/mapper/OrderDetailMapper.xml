<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <insert id="insertBatch">
        insert into order_detail (image, order_id, dish_id, setmeal_id, dish_flavor, name, number, amount) values
        <foreach collection="orderDetailList" item="od" separator=",">
            (#{od.image}, #{od.orderId}, #{od.dishId}, #{od.setmealId}, #{od.dishFlavor}, #{od.name}, #{od.number}, #{od.amount})
        </foreach>
    </insert>

    <!-- 查询商品Top10统计 -->
    <select id="getTop10ByMap" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name, sum(od.number) number from order_detail od left join orders o on od.order_id = o.id
        <where>
            <if test="beginTime != null">
               and o.order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                 and o.order_time &lt;= #{endTime}
            </if>
            and o.status = 5
        </where>
        group by od.name
        order by number desc
        limit 0,10
    </select>
</mapper>